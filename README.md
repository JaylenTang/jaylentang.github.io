# Jialin (Jaylen) Tang

Personal academic homepage for Jialin (Jaylen) Tang.

Website: https://jaylentang.github.io/

Built with Jekyll and GitHub Pages.

## Local Development

Install the Ruby dependencies:

```bash
bundle install
```

Run the site locally:

```bash
bundle exec jekyll serve -l -H localhost
```
